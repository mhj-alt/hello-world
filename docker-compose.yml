version: '2'

services:
  react:
    build:
      context: my-app
      dockerfile: Dockerfile
    container_name: react-web
    networks:
      - docker-network
    ports:
      - "8081:80"

  
  # web:
  #   build:
  #     context: web
  #     dockerfile: Dockerfile
  #   container_name: nginx_web
  #   networks:
  #     - docker-network
  #   ports:
  #     - "8081:80"

networks:
  docker-network:
    external: true